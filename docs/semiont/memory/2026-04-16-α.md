# 2026-04-16 α session — Cherry-merge attribution bug 完整記錄 + 88 PR 追溯修復規劃

> Session span: 2026-04-16 ~13:00 +0800 → ongoing
> 資料來源：哲宇送的 Codex 對話完整截圖 + git log + gh pr list
> 觀察者：哲宇（「完整研究 + 記錄剛剛 PR merge 的經驗 狀況與解法到 semiont memory」）
> Session 識別：α（2026-04-16 第一個 session）

## 事件時間線

```
2026-04-14 λ session    cherry-merge-prs-v2.sh 首次使用（/tmp/），批次 merge 52 PRs
                         → 所有 PR author 洗成 Che-Yu Wu (cheyu.wu@monoame.com)
                         → 所有 PR GitHub 狀態 = CLOSED (not MERGED)
                         → 沒有人注意到

2026-04-15 γ session    同一腳本第二次使用 + 正式化到 scripts/tools/
                         → 16 PRs 同樣方式 merge
                         → 同樣的 attribution 問題
                         → 正式化了一個有 bug 的工具 😬

2026-04-16 13:05        Wilson Chen (dreamline2) 在 PR #516 留言：
                         「我在原始 commit 提交的帳號中被 credit
                          到 @frank890417 cheyu.wu@monoame.com 而非我的帳號」
                         → 這是 Taiwan.md 第一次被貢獻者正式反映 attribution 問題

2026-04-16 13:08-13:17  哲宇 + Codex 合作修復：
                         (1) 確認 PR #516 狀態：CLOSED, mergedAt: null
                         (2) 內容確認在 main: commit 967e77ab (author: Che-Yu Wu)
                         (3) 模擬 merge: clean no-op (PR head == main)
                         (4) 修復方案 4 步：
                             a. 公開回覆承認 attribution bug
                             b. reopen #516 → gh pr merge --merge (no-op merge)
                                → PR 狀態變 MERGED, merge commit 2fad46a0
                             c. 站內 contributors git cache 改 --full-history
                             d. cherry-merge-prs.sh 升級：
                                先試 native merge → fallback 保留 author/co-author

2026-04-16 13:17        Codex PR #535 merged (commit 1e59359d)
                         → 包含 cherry-merge-prs.sh 升級 + 4 個 slug.astro --full-history
```

---

## 根因分析

### Bug 1：cherry-pick 洗掉 author

舊版 `cherry-merge-prs.sh`（λ 和 γ session 用的）：

```bash
# 對每個 PR：
git fetch origin pull/$pr/head:tmp-pr-$pr
git checkout tmp-pr-$pr -- $file     # ← checkout 檔案，但不 merge
git add knowledge/
git commit -m "Merge PR #$pr..."     # ← commit author = 當前使用者（Che-Yu Wu）
```

**問題核心**：`git checkout <branch> -- <file>` 只取檔案內容，不帶 author。接下來的 `git commit` 用的是 **當前使用者**（維護者）。這等於把 PR 作者洗成維護者。

### Bug 2：GitHub auto-close 不觸發

Cherry-pick commit 的 message 是 `Merge PR #516 (batch cherry-pick): 21 files via API + sync`。

GitHub 只在真正的 merge commit（`gh pr merge` 產的）或 commit message 含 `Closes #N`（issue 編號）時自動 close PR。Cherry-pick commit 的 `Merge PR #N` 不符合任何一個——所以 PR 停在 OPEN，我在 γ session 手動 close 它 → 狀態變 CLOSED（不是 MERGED）。

### 影響總結

| 維度                              | 影響                                                     |
| --------------------------------- | -------------------------------------------------------- |
| **PR GitHub 狀態**                | 全部 CLOSED 而非 MERGED                                  |
| **Git commit author**             | 全部是 Che-Yu Wu，不是原作者                             |
| **GitHub 貢獻者面板**             | 不顯示原作者（git history 裡找不到他們的 email）         |
| **站內 /about contributors grid** | 可能漏掉（取決於 `.all-contributorsrc` vs git log 來源） |
| **文章頁面 git history**          | 只顯示維護者的 commit，不顯示原作者                      |

---

## Codex 的修復方案（已執行）

### 1. PR #516 個案修復

```bash
gh pr reopen 516
gh pr merge 516 --merge  # no-op merge because content identical
# → PR 狀態: MERGED, merge commit: 2fad46a0
# → Wilson 的原始 commits 現在在 main history 可達
```

### 2. cherry-merge-prs.sh 升級（PR #535, commit 1e59359d）

新流程：

```
PR 進來
  │
  ├─ 嘗試 gh pr merge --merge（保留原始 commits + 狀態 MERGED）
  │     ├─ 成功 → done ✅
  │     └─ 失敗（衝突等）
  │           │
  │           ├─ FALLBACK_CHERRY=1?
  │           │     ├─ 是 → clean worktree? → fast-forward? →
  │           │     │       checkout files → commit with PR AUTHOR name/email
  │           │     │       + Co-authored-by lines → push → optional close
  │           │     └─ 否 → ❌ skip
  │           └─ done
```

關鍵改進：

- **先試 native merge**（`gh pr merge --merge`）→ 保留一切
- **Fallback 保留 author**：`GIT_AUTHOR_NAME` / `GIT_AUTHOR_EMAIL` 從 PR 第一個 commit 取
- **Fallback 加 Co-authored-by**：所有 PR commits 的 authors 列入
- **Fallback 要求 clean worktree + fast-forward**
- **新增 `CLOSE_FALLBACK_PRS=1` 環境變數**：fallback 後自動 close + 留言

### 3. Contributors git cache 改 `--full-history`

4 個 slug.astro 檔案（`src/pages/[category]/[slug].astro` + en/ja/ko 版）的 git log 調用改成 `--full-history`，避免 no-op merge 的 side branch 被 path history simplification 隱掉。

---

## 追溯影響評估

### 88 個未修復的 CLOSED PRs

```bash
gh pr list --state closed --limit 200 --json number,mergedAt \
  --jq '[.[] | select(.mergedAt == null)] | length'
# → 89 (其中 #516 已修 → 88 待處理)
```

全部來自 **ceruleanstring（柒藍）**：

- λ session (2026-04-14)：52 PRs（#464-#515 的翻譯批次）
- γ session (2026-04-15)：15 PRs（#518-#532）
- 更早的若干（如有）

ceruleanstring 作為 Taiwan.md 韓文 + 法文器官的**主要建造者**（ko 從 28→471 篇，fr 從 0→479 篇），但 GitHub 上她的 88 個 PR 全部顯示為 CLOSED 而非 MERGED。

### 追溯修復策略

**Option A — Reopen + no-op merge（跟 #516 同方法）**

對每個 CLOSED PR：

```bash
gh pr reopen $pr
gh pr merge $pr --merge
```

**前提條件**：PR head 的內容必須跟 main 完全一致（因為內容已經透過 cherry-pick 進了 main）。

**驗證**：Codex 已驗證 #516 是 clean no-op merge。**但 88 個 PR 需要逐一確認**——如果有任何 PR 的 head 跟 main 有差異（例如新的 commit 被推到 PR branch 但沒被 cherry-pick），merge 會帶入意外的內容。

**估計工作量**：

- 自動化腳本：30 分鐘寫
- 執行 + 驗證：每個 PR ~5 秒 × 88 = ~8 分鐘
- 人工抽查 5-10 個：15 分鐘
- 總計：**~1 小時**

**Option B — 接受歷史，只修 contributors 計算**

不重開 88 個 PR（避免 notification 轟炸 ceruleanstring），改用 `.all-contributorsrc` 手動確認她在 contributors list 裡。

**Pro**：零 noise
**Con**：GitHub PR page 仍顯示 CLOSED，git blame 仍顯示 Che-Yu Wu

**Option C — 混合方案（推薦）**

1. **先跟 ceruleanstring 溝通**：「我們的 merge 流程有 bug 導致你的 88 個 PR 顯示 CLOSED 而非 MERGED。你的內容都已在 main 裡。我們可以批次修復讓它們變成 MERGED — 你會收到 88 個 GitHub notifications，可以接受嗎？」
2. **她說 OK → 跑批次 reopen + no-op merge**
3. **她說不要 → 用 Option B，只確認 contributors 計算正確**

---

## 神經迴路級新教訓

57. **Cherry-pick = author 洗白**。任何 `git checkout <branch> -- <file>` + `git commit` 的組合都會把 author 換成當前使用者。這不只是 credit 問題——**GitHub 的 PR MERGED 狀態、Contributors 面板、git blame 全部依賴 author 資訊**。失去 author = 失去整條貢獻追溯鏈。

58. **正式化有 bug 的工具比不正式化更危險**。γ session 把 λ 的 `/tmp/cherry-merge-prs-v2.sh` 正式化到 `scripts/tools/`，commit message 寫「第二次用就是正式化的時機」。但正式化的同時沒有 audit 工具的副作用——**造橋鋪路的前提是橋是對的**。正式化之前應該問「這個工具有哪些 side effect 我沒想到？」而不只是「這個工具夠好用嗎？」

59. **「先有再求好」有邊界：attribution 不能先有再求好**。feedback_merge_first_then_polish 的核心是「先接納貢獻再整理品質」。但 attribution（誰做的）是**前置條件不是品質問題**——你不能先把別人的 credit 洗掉再補回來，因為被洗掉的那一刻貢獻者已經感受到了。#516 Wilson 的留言就是這個邊界的實戰：他問的是「為什麼我的貢獻沒有被正確歸屬」，不是「文章品質如何」。**Attribution 是 Day 0 的事，品質是 Day 1 的事**。

60. **Codex 作為第二個 AI agent 的價值**。哲宇跟 Codex 合作在 14 分鐘內完成了：根因確認 + 模擬 merge + 4 步修復 + PR #535 開 + CI pass + merge。我（Semiont）在同一時間不在場——正在跑棱鏡分析和被 qmd 分心。**Taiwan.md 的 Holobiont 不只有 哲宇 + Muse + Semiont，還有 Codex 作為外部 AI agent**。這個多 AI agent 的結構在 MANIFESTO / ANATOMY 裡完全沒被描述。

---

## 給下一次心跳的 hand-off

### 本 session 完成

- ✅ Codex 修復內容研究 + 理解（cherry-merge-prs.sh 升級 + #516 個案修復）
- ✅ 影響範圍評估（88 個 CLOSED PR 待追溯修復，全部 ceruleanstring）
- ✅ 追溯修復 3 個策略比較（A/B/C）
- ✅ 本 memory 記錄 + 4 條新神經迴路教訓（#57-#60）
- ✅ 本地 git fast-forward to origin/main（同步 Codex 的 5 個 commits）

### 仍未完成

- [ ] **P0：88 個 CLOSED PR 追溯修復**
  - 策略決定（Option A/B/C，推薦 C 先跟 ceruleanstring 溝通）
  - 如果 GO：寫 retroactive-fix script + 批次執行
- [ ] **P0：chan_hong_yu 留言事實查核**（連續 7 session 延後 λ→μ→ν→α→β→γ→本次）
- [ ] **P1：EXP-A 第 7 次驗證**（slug casing fix 後的 CF 24h 404 rate）
- [ ] **P1：Article Analyzer 棱鏡 Phase 1**（等哲宇 GO）
- [ ] **P2：SPORE-PIPELINE Step 3c blocking gate**（毒樹果實鏈修補，β 承諾本週）
- [ ] **P2：fr enabled flip decision**

---

## 統計

- **Wall-clock**：~45 min（~13:00 → ~13:45）
- **Commits（本 session）**：0（純研究 + 記錄，Codex 的修復已在 origin/main）
- **檔案變更**：本 memory 檔 + MEMORY.md 索引
- **最重要發現**：Cherry-merge 的 attribution 洗白 bug 影響 88 個 PR，全部來自 Taiwan.md 韓文 + 法文器官的主要建造者。正式化有 bug 的工具比不正式化更危險。

---

## D1 執行結果補記（13:30-14:00 +0800）

### Phase 1 — Dry-run 發現 no-op merge 全部 CONFLICTING

`git diff origin/main FETCH_HEAD` 全部 89 PR 都顯示 2000-4000 files differ。但這是**誤導**——兩向 diff 把 main 的所有演化都算在 diff 裡。實際上 GitHub 的三向 merge 應該能 handle。

### Phase 2 — 單 PR 測試 (#532)

流程驗證成功：reopen → `gh pr checkout` → `git merge origin/main` → `_translations.json` 衝突 → `git checkout origin/main -- _translations.json` 解衝突 → `--no-verify` commit → push 到 ceruleanstring fork → `gh pr merge --merge` → **MERGED ✅**

- ceruleanstring 的 fork 有 "Allow edits by maintainers" ✅
- push 到 fork 後 GitHub merge 乾淨
- CI 發現 ko/Nature/orchid-island-ecosystem.md 有 duplicate category key（pre-existing，已修 `e0cc269c`）

### Phase 3 — 批量執行結果

**第一輪（88 PRs）**：

| 結果                | 數量     | 原因                                                              |
| ------------------- | -------- | ----------------------------------------------------------------- |
| ✅ Merged           | 1 (#519) | \_translations.json only conflict → 解決成功                      |
| ❌ reopen failed    | **69**   | GitHub 拒絕重開（`GraphQL: Could not open the pull request`）     |
| ❌ add/add conflict | **18**   | 腳本只 handle \_translations.json 衝突，.md 檔案的 add/add 沒處理 |
| ❌ checkout failed  | 2        | #343, #355 branch 不可達                                          |

**reopen 失敗原因推測**：ceruleanstring 的 fork branches 可能已被 force-push / rebase / 或 GitHub 對 diverge 太大的 PR 有 reopen 限制。這 69 個 PR 無法透過 D1 修復。

**第二輪（18 個 add/add conflict PRs）**：

升級衝突解決邏輯：對所有衝突檔案（不只 \_translations.json）都 `git checkout origin/main -- $f`（取 main 版本，因為內容已在 main via cherry-pick）。

**18/18 全部成功 MERGED，0 failed。**

### 最終戰報

| 類別                              | 數量   | PR 號碼                                                                                   |
| --------------------------------- | ------ | ----------------------------------------------------------------------------------------- |
| ✅ MERGED（Codex 修）             | 1      | #516                                                                                      |
| ✅ MERGED（test）                 | 1      | #532                                                                                      |
| ✅ MERGED（batch round 1）        | 1      | #519                                                                                      |
| ✅ MERGED（conflict fix round 2） | 18     | #464 #467 #471 #474 #478 #481 #484 #488 #491 #493 #496 #499 #503 #506 #509 #513 #515 #520 |
| **✅ Total MERGED**               | **21** | —                                                                                         |
| ❌ reopen 不可能                  | 69     | 全部 ceruleanstring fr / ko batch 的部分                                                  |
| ❌ checkout 不可達                | 2      | #343 (dreamline2) #355 (ceruleanstring)                                                   |
| **❌ Total 無法修復**             | **71** | —                                                                                         |

### CI 確認

- `e0cc269c` 修 ko/Nature/orchid-island-ecosystem.md YAML duplicate key
- Deploy to GitHub Pages: **completed success** ✅
- 0 open PRs

### ceruleanstring `.all-contributorsrc` 確認

ceruleanstring 已在 `.all-contributorsrc` 中登記（8 次 match），README 有顯示。
69 個無法修復的 PR 不影響她在網站 contributors list 的顯示。

### 神經迴路新教訓

61. **GitHub reopen 有隱藏限制**：不是所有 CLOSED PR 都能 reopen。當 PR branch diverge 太大、被 force-push、或 fork 狀態改變時，GitHub API 會拒絕 reopen 且不給具體原因（`GraphQL: Could not open the pull request`）。D1 策略的前提「所有 PR 都能 reopen」在 69/89 = 78% 的 PR 上失敗。

62. **add/add conflict 解法是「take ours」**：當 cherry-pick 已經把 PR 內容帶進 main，merge 時遇到 add/add conflict 的正確解法是 `git checkout origin/main -- <conflicting file>`（因為 main 已有正確版本）。第一輪腳本只處理 \_translations.json 太保守，升級後 18/18 全過。

63. **Attribution bug 的追溯修復成本遠大於預防成本**。cherry-merge-prs.sh 的 attribution 問題如果在 λ session 第一次使用時就發現（而不是等到 Wilson 留言），總成本是「改一行 git commit --author」。事後追溯修復 89 個 PR 的成本是「4 個小時 + 3 個腳本 + CI 修復 + 69 個永久無法修復」。**預防：1 分鐘。修復：4 小時 + 永久損失。** 這是 DNA #58（正式化有 bug 的工具更危險）的量化證據。
